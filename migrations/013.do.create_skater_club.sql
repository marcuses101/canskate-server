CREATE TABLE skater_club (
  id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  skater_id INTEGER REFERENCES skaters(id) NOT NULL,
  club_id INTEGER REFERENCES clubs(id) NOT NULL,
  UNIQUE (skater_id, club_id)
)