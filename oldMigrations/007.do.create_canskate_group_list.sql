CREATE TABLE canskate_group_list (
  id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  skater_id INTEGER REFERENCES skaters(id) ON DELETE CASCADE NOT NULL,
  group_id INTEGER REFERENCES canskate_groups(id) ON DELETE CASCADE NOT NULL,
  UNIQUE (skater_id, group_id)
);